<Window x:Class="God.Support.mode.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="BookStore God Mode"
        MinHeight="800" MinWidth="1280"
        Height="800" Width="1280"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <materialDesign:DialogHost>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="#1a1a1a">
                <DockPanel>
                    <!-- App Title -->
                    <Border DockPanel.Dock="Top" Padding="20,24,20,16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="BookOpenPageVariant" Width="32" Height="32"
                                                         Foreground="#FF5722" VerticalAlignment="Center"/>
                                <TextBlock Text="AlkotasokTara" FontSize="20" FontWeight="Bold" 
                                           Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="God Mode" FontSize="12" Opacity="0.5" Margin="42,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <Separator DockPanel.Dock="Top" Background="#333"/>

                    <!-- Navigation -->
                    <StackPanel Margin="0,8,0,0">
                        <StackPanel.Resources>
                            <Style x:Key="NavBtn" TargetType="RadioButton" BasedOn="{StaticResource MaterialDesignTabRadioButtonBottom}">
                                <Setter Property="Height" Value="48"/>
                                <Setter Property="Padding" Value="20,0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#CCC"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Margin" Value="0,2"/>
                            </Style>
                        </StackPanel.Resources>

                        <RadioButton Style="{StaticResource NavBtn}" IsChecked="True"
                                     Click="NavButton_Click" Tag="0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboard" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Attekintes" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton Style="{StaticResource NavBtn}" Click="NavButton_Click" Tag="1">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountGroup" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Felhasznalok" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton Style="{StaticResource NavBtn}" Click="NavButton_Click" Tag="2">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="BookMultiple" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Konyvek" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton Style="{StaticResource NavBtn}" Click="NavButton_Click" Tag="3">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="SwapHorizontal" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Kolcsonzesek" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton Style="{StaticResource NavBtn}" Click="NavButton_Click" Tag="4">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CreditCard" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Rendelesek" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton Style="{StaticResource NavBtn}" Click="NavButton_Click" Tag="5">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cart" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Kosarak" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton Style="{StaticResource NavBtn}" Click="NavButton_Click" Tag="6">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cog" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                <TextBlock Text="Beallitasok" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Column="1">
                <ContentControl Content="{Binding CurrentViewModel}" Margin="0"/>

                <!-- Snackbar -->
                <materialDesign:Snackbar MessageQueue="{Binding MessageQueue}"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Bottom"
                                         Margin="0,0,0,12"/>
            </Grid>
        </Grid>
    </materialDesign:DialogHost>
</Window>
